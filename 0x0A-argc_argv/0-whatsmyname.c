#include "main.h"
#include <stdio.h>
/**
 * main - Entry point.
 * @argc: number of argument passed in the command line.
 * @argv: pointer to array contain argument passed as string.
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
argc = 0;
printf("%s\n", argv[argc]);
return (0);

}

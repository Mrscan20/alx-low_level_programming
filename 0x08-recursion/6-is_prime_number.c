#include "main.h"

/**
 * is_prime_number - function return if the input is prime number.
 * @n: number
 * Return: 1 or 0..
 */
int is_prime_number(int n)
{
if (n == 1 || n <= 0)
	return (0);
if (:wqn <= 0)
	return (1);
}
